% The class hardcodes pdfx's a-1b. Level u also guarantees every glyph maps back to Unicode, which
% text extraction depends on.
\PassOptionsToPackage{a-2u}{pdfx}
\documentclass[10pt,a4paper,withhyper,ragged2e]{altacv}
\usepackage{cvstyle}

\begin{document}

\name{Tom Dobson}
\tagline{Data Engineer}
\personalinfo{%
  \email{dobs.tx@gmail.com}
  \phone{+44 7968 673704}
  \location{London, UK}
  \linkedin{dobsontom}
  \github{dobsontom}
}
\makecvheader

\cvsection{Summary}

Data engineer owning Tails.com's data platform end to end, from the MySQL and Postgres databases
behind the application, through the Snowflake, dbt, and Airflow estate, to the Terraform and
Kubernetes underneath. Cut Snowflake spend 46\% and ETL licence spend 94\%, together over \$160k a
year, whilst the estate grew to around 290~billion rows. Inside the application, profiled the
production database and cut its query rate 40\% and its CPU 28\% across 2026. Looking for a senior
data engineering role where the infrastructure is part of the job.

\cvsection{Experience}

\cvrole{Data Engineer}{Tails.com}{Mar 2026 -- present}
\begin{itemize}
  \item Cut Snowflake spend \cvmetric{46\%} to its lowest month on record, saving around \$100k a
        year, by removing unused and over-scheduled compute and sizing each warehouse on measured
        workload
  \item Migrated ingestion from Fivetran, Stitch, and Meltano to self-hosted Airbyte on EKS,
        cutting ETL licence spend \cvmetric{94\%}, around \$64k a year
  \item Cut the production MySQL database's query rate \cvmetric{40\%} and CPU 28\% by profiling
        its workload and fixing the worst offenders, including missing indexes, dead scheduled
        jobs, and N+1 lookups that refetched whole tables over 100~times a second
  \item Mapped the whole data platform's architecture as code and keep it current, around
        \cvmetric{90}~components from source to destination, which the Technology Director called
        the standard every team should follow
  \item Moved Aurora MySQL and Postgres access from shared static credentials, readable by any
        Parameter Store user, to per-person AWS IAM authentication with 15-minute tokens and
        role-scoped grants, so every production query traces to an engineer
  \item Designed and built Snowflake governance as code in Terraform, with access managed from
        Google Groups through to Snowflake roles and all \cvmetric{15}~service accounts moved from
        passwords to key-pair authentication
  \item Audited every event path from the core application into Snowflake ahead of a platform
        migration, found the one dependency with no replacement, and wrote the reference backend
        engineers used to design the Kafka topic replacing it
  \item Secured AI-agent access to dbt and Snowflake through an MCP server on a dedicated identity
        that can build in each developer's own database but never read unmasked PII, write to
        production, or escalate its own privileges
  \item Built \cvmetric{six} new Airflow pipelines, landing survey and support-ticket sources in
        Snowflake and pushing customer segments and orders back into CX and marketing tools
  \item Created and chair a biweekly data infrastructure meeting, running since March 2025, for the
        whole data team including its head, where the team agrees platform changes, audits
        Snowflake, ingestion, and dbt spend, and prioritises the work that follows
\end{itemize}

\cvrole{Analytics Engineer}{Tails.com}{Oct 2024 -- Mar 2026}
\begin{itemize}
  \item Owned a dbt project of around \cvmetric{1,600} models on Snowflake and refactored it onto
        dbt's recommended conventions, moving every model into staging, intermediate, and marts
        layers, standardising naming, linting, and grants, and carrying the breaking changes
        through eight repos belonging to other teams
  \item Sole maintainer of Airflow on EKS: migrated \cvmetric{12}~DAGs off v1, took it through
        every upgrade to v3.3, and cut image builds from 7~minutes to under 30~seconds by no longer
        compiling the image from source on every run
  \item Removed over \cvmetric{150,000}~model builds a month, deprecating 400~dbt models on
        measured build cost and downstream usage, and preventing rebuilds before upstream data is
        refreshed
  \item Cut dbt CI time \cvmetric{74\%}, from 14~minutes to around~3, by deferring unchanged models
        to their production tables rather than rebuilding them, and capping how far downstream each
        change is tested
  \item Brought every model into YAML metadata, up from 21\%, and took documented columns from
        under 1,000 to over \cvmetric{12,000}, enforced by dbt-score, which fails CI on any touched
        resource not fully documented and owned
  \item Refactored and incrementalised the costliest dbt models, cutting the worst from up to~28
        minutes to under \cvmetric{3} by inverting logic that built every pet and product
        combination, validated row for row against the original
  \item Rebuilt dbt alerting on Elementary, tuned so only the \cvmetric{7.5\%} of detections that
        need action reach an engineer, plus freshness checks that catch silent ingestion failures
  \item Rebuilt deployment and test workflows onto a shared definition of runner, authentication,
        and dbt setup, cutting their YAML by \cvmetric{48\%} whilst their number grew, and
        parallelised linting, scoring, and compilation
\end{itemize}

\cvrole{BI Consultant, contract via The Information Lab}{Viasat}{Jan 2024 -- Oct 2024}
\begin{itemize}
  \item Built a revenue-protection reporting system in BigQuery that exposed over \cvmetric{£2.9m}
        of annualised revenue at risk, and led the ETL migration from Alteryx to GCP, saving around
        £100k a year in licences
  \item Built a basket-abandonment pipeline on BigQuery feeding Adobe Campaign, and a performance
        data mart consolidating 15~tables behind Tableau alerting
\end{itemize}

\cvrole{Analytics Consultant}{The Information Lab}{Apr 2022 -- Aug 2024}
\begin{itemize}
  \item Automated IAG Cargo's warehouse task distribution with Oracle stored procedures, saving
        around \cvmetric{1 FTE}, and redesigned the schema to take reporting discrepancies from
        15\% to under 1\%
  \item Built pipelines for the BBC consolidating over 6m~rows of survey data behind a self-service
        Tableau front end
  \item Automated the detection and publicising of Tableau User Group events with Python,
        Snowflake, and GitHub Actions, lifting engagement around \cvmetric{80\%}
\end{itemize}

\cvsection{Skills}

\cvskillline{Data platform}{Snowflake, dbt, Airflow, Airbyte (self-hosted), Snowpipe, Kafka as a
  consumer}
\cvskillline{Infrastructure}{Kubernetes (EKS, Helm, GitOps), OpenTofu and Terraform, AWS (IAM, RDS
  and Aurora, S3, Identity Center), Docker, GitHub Actions}
\cvskillline{Application databases}{MySQL and Aurora, PostgreSQL, \cvcode{performance\_schema},
  Percona Toolkit}
\cvskillline{Languages}{SQL, Python (SQLAlchemy, Alembic, Flask, pandas)}
\cvskillline{Observability}{Datadog APM, Grafana, Elementary}
\cvskillline{Prior}{Tableau, Alteryx, Power BI, GCP (BigQuery), Redshift, Oracle, R}

\cvsection[nodivider]{Education}

\cvrole{MSc by Research, Biological Sciences}{University of Bristol}{2020 -- 2022}
\href{https://hdl.handle.net/1983/1eb946be-6487-44d3-911c-04aeb6a3f258}{Runner-up for the
  Postgraduate Thesis Prize, judged across MSc and PhD theses}

\cvrole{BSc (Hons) Biology, First Class}{University of Bristol}{2016 -- 2019}

\cvsection[nodivider]{Training}

\cvrole{AI Bootcamp}{STX Next}{Jun 2026}
Four weeks of hands-on AI-assisted delivery, from planning and context engineering to LLM and MCP
integration

\end{document}
